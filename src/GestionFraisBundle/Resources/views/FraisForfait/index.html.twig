{% extends '::base.html.twig' %}

{% block body -%}
    <h1>FraisForfait list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libellefraisforfait</th>
                <th>Montant</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('fraisforfait_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.libelleFraisForfait }}</td>
                <td>{{ entity.montant }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('fraisforfait_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('fraisforfait_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('fraisforfait_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}

{% extends '::base.html.twig' %}

{% block body -%}
    <h1>FraisForfait</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Libellefraisforfait</th>
                <td>{{ entity.libelleFraisForfait }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ entity.montant }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('fraisforfait') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('fraisforfait_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}

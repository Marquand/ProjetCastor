{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Visiteur creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('visiteur') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
